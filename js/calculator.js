let num_1= 20;
let num_2= 20;
let operator="+";

function calc(...arr) {
    let n1=arr[0];
    let n1=arr[1];
    let n1=arr[2];
    
    switch(s){
        case '+'=
        return n1+n2 ;
        default:
            return 'please enter the valid value & operator's '
    }
}